<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-neweaten="{{devoured}}">
		{{#if devoured}}Eat It Again{{else}}EAT BURGER!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
